[pytest]
addopts = -q --maxfail=1 --disable-warnings --cov=app --cov-report=term-missing --cov-report=xml --cov-config=.coveragerc
testpaths = tests
asyncio_mode = auto
filterwarnings =
    ignore::DeprecationWarning
